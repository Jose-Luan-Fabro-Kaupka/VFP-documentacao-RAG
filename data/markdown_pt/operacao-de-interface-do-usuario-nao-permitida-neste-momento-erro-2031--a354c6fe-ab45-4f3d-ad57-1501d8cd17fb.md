# Operação de interface do usuário não permitida neste momento. (Erro 2031)

Este erro é gerado quando o modo de servidor não assistido está habilitado com SYS(2335). Se possível, remova do seu servidor o código que cria o estado modal que gera este erro.
