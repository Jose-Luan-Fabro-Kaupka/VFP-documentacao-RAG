# A janela não foi definida com DEFINE WINDOW (Erro 214)

Foi feita uma tentativa de ativar uma janela que não foi definida.

Use o comando DEFINE WINDOW Command antes de usar o comando ACTIVATE WINDOW Command.
