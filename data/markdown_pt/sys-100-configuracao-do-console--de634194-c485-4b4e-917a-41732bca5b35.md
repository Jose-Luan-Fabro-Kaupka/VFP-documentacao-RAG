# SYS(100) - Configuração do console

Incluído para compatibilidade com versões anteriores. Use SET("CONSOLE") em vez disso.

```foxpro
SYS(100)
```

# Valores de retorno

Character

# Exemplo

```foxpro
IF SYS(100) != 'ON'
   SET CONSOLE ON
ENDIF
```
