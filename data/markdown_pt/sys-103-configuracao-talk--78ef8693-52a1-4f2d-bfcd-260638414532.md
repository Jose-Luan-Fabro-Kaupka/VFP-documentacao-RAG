# SYS(103) - Configuração TALK

Incluído para compatibilidade com versões anteriores. Use SET("TALK") em vez disso.

```foxpro
SYS(103)
```

# Valores de retorno

Character

# Exemplo

```foxpro
IF SYS(103) != 'ON'
   SET TALK ON
ENDIF
```
