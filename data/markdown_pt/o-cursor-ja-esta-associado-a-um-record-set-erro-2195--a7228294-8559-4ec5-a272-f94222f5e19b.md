# O cursor já está associado a um record set (Erro 2195)

Este erro pode ser gerado por VfpOleDB.dll, o OLE DB Provider for Visual FoxPro, quando a função SETRESULTSET( ) retorna um cursor já associado a um record set.
