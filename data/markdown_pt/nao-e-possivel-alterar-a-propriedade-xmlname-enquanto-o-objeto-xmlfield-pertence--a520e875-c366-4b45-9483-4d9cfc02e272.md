# Não é possível alterar a propriedade XMLName enquanto o objeto XMLField pertence ao objeto XMLTable. (Erro 2103)

O Visual FoxPro não permite que a propriedade XMLName de um objeto XMLField seja alterada quando o objeto XMLField pertence a um objeto XMLTable.
