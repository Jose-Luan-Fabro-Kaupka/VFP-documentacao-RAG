# A classe não pode ser instanciada porque o membro "name" tem número incorreto de parâmetros (Erro 1442)

A cláusula IMPLEMENTS no comando DEFINE CLASS usa um número incorreto de parâmetros para esta classe. Para obter mais informações, consulte Comando DEFINE CLASS.
