# Aviso: a chave definida pela propriedade KeyField para a tabela "alias" não é exclusiva (Erro 1495)

A chave especificada para esta tabela alterou mais de uma linha na tabela base para algumas das linhas da exibição remota sendo atualizadas.

Redefina a chave usando a propriedade KeyField da exibição.
