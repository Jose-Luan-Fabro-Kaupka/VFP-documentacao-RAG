# O formato XML aninhado é incompatível com o modo "Changes Only" (Erro 2179)

Este erro é gerado pelo método ToXML da classe XMLAdapter se tanto a propriedade RespectNesting quanto o parâmetro lChangesOnly estiverem definidos como true (.T.).
